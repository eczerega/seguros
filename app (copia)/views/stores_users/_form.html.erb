<%= form_for(@stores_user) do |f| %>
  <% if @stores_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stores_user.errors.count, "error") %> prohibited this stores_user from being saved:</h2>

      <ul>
      <% @stores_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :store_id %><br>
    <%= f.text_field :store_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
